# Required Environment Variables for the Next.js Web Application

# Supabase Project URL
# Found in Supabase Project Settings -> API
SUPABASE_URL=""

# Supabase anonymous key (use for auth/session)
SUPABASE_ANON_KEY=""
# Expose anon key to the client if needed for browser-side helpers
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Supabase Service Role Key (secret)
# Found in Supabase Project Settings -> API -> Project API keys -> service_role (Secret)
# WARNING: Do NOT expose this key to the client-side.
SUPABASE_SERVICE_KEY=""

# Resend API Key for sending emails
# Get one from https://resend.com/
RESEND_API_KEY=""

# Email address to send confirmation emails from
# Must be a verified sender in Resend (include display name)
RESEND_FROM_EMAIL="Konferenca SHFK <konferenca@shfk.org>"
RESEND_WEBHOOK_SECRET=""

# Private Key in PEM format for signing QR code tokens
# This should be a securely generated RSA private key.
# Example (for development, use a proper key management in production):
# QR_PRIVATE_KEY_PEM="-----BEGIN RSA PRIVATE KEY-----
# MIIEpAIBAAKCAQEAs...
# -----END RSA PRIVATE KEY-----"
# If not provided, a temporary key pair will be generated in demo mode (console warning).
QR_PRIVATE_KEY_PEM=""

# Admin secret for specific administrative functions (x-admin-key header)
ADMIN_SECRET_KEY="some-long-random-string"
# Deprecated fallback:
# ADMIN_SECRET="some-long-random-string"

# Public base URL of the deployed site (e.g., https://shfk.org)
NEXT_PUBLIC_BASE_URL="https://shfk.org"

# Conference slug for which the registrations are managed.
# Default is "annual-conference"
CONFERENCE_SLUG="annual-conference"

# Scanner download links
SCANNER_ANDROID_URL=""
SCANNER_IOS_URL=""
